# 路线图 Roadmap

CRT Retro Filter 的未来更新规划。按「短期 / 中期 / 长期」划分，均以不破坏现有逻辑、保持向后兼容为原则。

> 状态图例：⬜ 未开始 · 🔜 已排期 · ✅ 已完成

---

## 一、短期（v3.7.x 系列 · 功能补强）

### 1.1 动画专项增强 🔜
- **逐帧故障演变**：故障图案随帧号变化（`frameIndex` 参与随机种子），让"全部帧应用"产生每帧不同的撕裂/位移，模拟 datamosh 风格的动态故障
- **VHS 滚动条动画**：跟踪条位置随帧滚动，模拟录像带走带时的持续漂移
- 与现有"固定噪点 / 确定性故障"互补：用户可选"每帧固定"或"每帧演变"

### 1.2 预设导入 / 导出 🔜
- 自定义预设导出为 JSON 文件，支持导入分享
- 配套：GitHub 社区分享帖模板、预设文件名规范

### 1.3 应用方式面板整理
- 将「复制图层 / 只应用到选区 / 全部帧」合并为一个「应用方式」分组，视觉更紧凑

### 1.4 工程：测试入库 + CI 校验
- 把当前回归测试（强度/选区/全部帧/glitch/菜单）收进仓库 `tests/`
- CI 增加：`luacheck` 静态检查 + 测试脚本在 Ubuntu runner 上跑 Aseprite CLI 校验
- 效果图补全：6 张故障滤镜 + 数字故障预设的真实截图（替换占位图）

---

## 二、中期（v3.8–v3.9 · 深度功能）

### 2.1 更多主流 glitch 手法（延续 v3.5–3.6 的路线）
- **数据弯折 Data Bend**：按行对色带做非对称偏移（databend 风格）
- **镜像重复 Repeat**：切片复制粘贴到相邻位置（区别于现有的"镜像翻转"）
- **信号丢失 No Signal**：叠加静态噪波块 + NO SIGNAL 字样（需内置 7 段式字模）
- 新增前先做与现有 18 个滤镜的去重分析（沿用 v3.5 的检查流程）

### 2.2 每滤镜独立强度
- 在全局「预设强度」之外，为每个滤镜增加独立浓度滑块（0–100%），精细控制混合比例
- 全局强度变为"总闸"，两者相乘

### 2.3 批量与范围
- **处理所有图层**：一次应用到精灵的所有图层（当前仅当前图层 + 全部帧）
- 撤销策略改进：可选"每滤镜一步撤销" vs 默认"整链一步撤销"

### 2.4 性能专项
- 大画布（>1024px）预览自动降采样
- 像素排序等耗时滤镜的缓存（行选择结果按参数缓存）

---

## 三、长期（v4.0 · 平台化）

### 3.1 预设市场 / 一键分享
- 预设通过 GitHub Gist 分享，插件内一键导入（需要网络能力评估）
- 社区预设库页面（GitHub Discussions 或独立仓库）

### 3.2 多语言
- 在现有 EN/ZH 基础上扩展 ja / fr / es / ko（`lang.lua` 结构已支持，只需社区贡献）

### 3.3 官网 / 文档站
- 独立文档站（GitHub Pages）：效果图册、视频教程、FAQ 汇总、预设分享

### 3.4 与 Aseprite 生态联动
- 动画导出：配合 Aseprite 的导出功能输出故障风格 GIF
- 官方论坛「Scripts & Extensions」长期维护帖 + itch.io 页面更新

---

## 四、持续进行（与版本无关）

- **社区运营**：Bilibili / YouTube 视频系列（配合账号引流）、Bluesky / X 发版公告
- **CI 自动发版**：每次打 tag 自动出 `.aseprite-extension` + Release（已上线，保持维护）
- **Bug 反馈闭环**：README/FAQ 保留 Issues 链接，重大修复进 CHANGELOG
- **效果图资产**：新滤镜发布时同步产出真实截图（1280 宽）替换占位图

---

## 优先级建议

1. **1.1 动画专项**（差异化最强，像素动画用户最需要）
2. **1.2 预设导入导出**（社区分享的钥匙，成本低）
3. **2.2 每滤镜独立强度**（自由度提升大）
4. **1.4 测试入库 + CI 校验**（工程地基，随时可做）

> 每个版本都会走完整流程：实现 → 测试 → 版本号 + CHANGELOG → tag → CI 自动发版 → 本机同步。
